import styled from "styled-components";

export const HandDisplayerElement = styled.div`
  display: flex;
  flex-direction: row;
  padding: 1.5em;
  margin: 2em 0 0 0;
  overflow-x: scroll;
  width: fit-content;
  border-radius: 0.5rem;
  max-width: 95vw;
  box-shadow: rgba(20, 20, 70, 0.15) 0px 30px 60px -12px inset,
    rgba(0, 0, 0, 0.3) 0px 18px 36px -20px inset;
`;
